<input type="text"
       id="{{ $id or 'm_datepicker_1' }}"
       name="{{ $name or 'datepicker1' }}"
       class="form-control {{$class or ''}}"
       placeholder="{{ $placeholder or 'Select date' }}"
       style="{{$style or ''}}"
       readonly>
