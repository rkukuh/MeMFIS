<{{ $html_tag or 'span' }}
    class="{{$class or ''}}"
    style="font-weight: {{ $font_weight or 'bold' }};
    color:{{ $color or 'red' }};">

    {{ $text or '* (multiple)' }}

</{{ $html_tag or 'span' }}>
