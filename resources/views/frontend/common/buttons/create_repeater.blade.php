<div id="{{ $id or '' }}"
data-repeater-create="" class= "btn btn-{{ $color or 'brand' }} btn-{{ $size or 'sm' }} {{ $class or '' }}" style="{{ $style or '' }}" title="{{ $title or '' }}">
    <span>
        <i class="la la-plus"></i>
    </span>
</div>
