<div data-repeater-delete="" 
    class="btn
           btn-{{ $color or 'danger' }}
           btn-{{ $size or 'sm' }}
           {{ $class or '' }}"
           style="{{ $style or '' }}"
           title="{{ $title or '' }}">
        <span>
            <i class="la la-trash-o"></i>
        </span>
</div>